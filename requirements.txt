# Pinned to the environment that produces the model artifacts. These previously
# claimed versions (e.g. scikit-learn 1.4.2) that no local or CI environment
# actually used, so the container unpickled 1.5.2 estimators under 1.4.2 and
# emitted InconsistentVersionWarning -- a silent correctness risk at scoring time.
pandas==2.2.3
numpy==2.2.6
scikit-learn==1.5.2
scipy==1.14.1
xgboost==3.2.0
joblib==1.4.2
boto3==1.43.0
duckdb==1.5.3
dbt-core==1.11.14
dbt-duckdb==1.10.1
pydantic==2.11.8
streamlit==1.54.0
fastapi==0.141.1
uvicorn==0.35.0
# Security floors for transitive packages; supported by the parents above.
starlette==1.3.1
sqlparse==0.6.0
