name: 'aegis_agent'
version: '1.0.0'
config-version: 2

profile: 'aegis_agent'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  aegis_agent:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
