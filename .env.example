# Local demonstration only. Never copy development identity settings into deployment.
AEGIS_SECURITY_MODE=development
AEGIS_DEV_SUBJECT=local-investigator
AEGIS_DEV_ROLES=investigator
AEGIS_DEV_ORGANIZATION=local-demo
# The HTTP alert-ingestion role represents a verified scoring service. Do not grant
# alert_ingestor to an interactive production user.

# Local Phase 5 service binding. Production exposure is intentionally unsupported.
AEGIS_API_HOST=127.0.0.1
AEGIS_API_PORT=8000

# Optional path for the Phase 6 local governance registry. By default it lives beside
# MODELS_ARTIFACTS_DIR. Use model_governance_reviewer only for governance CLI sessions.
# AEGIS_MODEL_REGISTRY_PATH=models_artifacts/registry.json

# Non-secret runtime configuration.
AWS_DEFAULT_REGION=us-east-1
COMPLIANCE_S3_BUCKET=

# Do not commit AWS credentials, Clerk keys, session tokens, or JWTs. Production
# secrets must be injected by the deployment platform's secret manager.
